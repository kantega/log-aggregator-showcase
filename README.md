# log-aggregator-showcase
Companion code for the talk "Claude and the Mystery of Integrations." Demonstrates using Claude to generate tailored mock servers and test complex failure scenarios (like exponential backoff) for a log aggregator application.
